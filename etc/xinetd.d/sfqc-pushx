service sfqc-pushx
{
	disable	= no
	flags		= REUSE NORETRY NODELAY IPv4
	socket_type	= stream
	wait		= no
	user		= devuser
	server		= [sfq command install path]/sfqc-pushx
	server_args	= -q -p adda
}
